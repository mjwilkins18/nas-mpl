Welcome to the Parallel ML implementations of NPB!

In each benchmark folder, run the following command to compile a benchmark for a particular class size:
```
make compile CLASS=X
```

To run a benchmark you have compiled...
```
make run CLASS=X
```
